import PRODUCTS from '../../data/dummy-data'

const initialState={
    availableProducts: [],  // all products
    userProducts: [],  // products that the user created 


};
// Redux 
export default (state = initialState, action ) =>{
    return state
}